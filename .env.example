# ============================================
# Emerald Cash VMS - Environment Template
# ============================================
# Never commit real secrets. Use this file as a template only.

# Public app URL used for CORS/security headers (production domain)
NEXT_PUBLIC_APP_ORIGIN=https://emeraldcash-vms-next.vercel.app

# Google Apps Script endpoint (required)
NEXT_PUBLIC_API_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec

# Optional server-side fallback endpoint (if different from NEXT_PUBLIC_API_URL)
APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec

# Upload token used by Apps Script image upload action
APPS_SCRIPT_UPLOAD_TOKEN=replace_with_apps_script_upload_token

# Session secret for cookie signing (required, 32+ random chars)
SESSION_SECRET=replace_with_a_64_char_random_secret

# Optional auth users (bcrypt hashes)
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=$2b$10$replace_with_admin_bcrypt_hash
STAFF_USERNAME=staff
STAFF_PASSWORD_HASH=$2b$10$replace_with_staff_bcrypt_hash

# Optional: pre-seed multiple users as JSON array
# AUTH_USERS_JSON=[{"username":"admin","role":"Admin","passwordHash":"$2b$10$..."},{"username":"staff1","role":"Staff","passwordHash":"$2b$10$..."}]

# Optional API compatibility mode for Apps Script integrations
NEXT_PUBLIC_USE_QUERY_TOKEN=false

# Optional cron protection token (if cron endpoint is enabled)
CRON_SECRET=replace_with_cron_secret

# Vercel auto-injected variable (do not set locally unless needed)
# NEXT_PUBLIC_VERCEL_URL=emeraldcash-vms-next.vercel.app

# Local mobile testing helper (optional)
# DEV_LAN_IP=192.168.1.10
