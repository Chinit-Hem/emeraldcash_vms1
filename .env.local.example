# ============================================
# EC-VMS Environment Variables
# ============================================

# Required: Google Apps Script API URL
# This is the URL of your deployed Google Apps Script
# Get this from: https://script.google.com/home/deployments
NEXT_PUBLIC_API_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# Required: Session secret for authentication
# Generate a random string (e.g., using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=your-random-secret-here-minimum-32-characters-long

# Optional: Use query token instead of Bearer header (for Apps Script compatibility)
# Set to "true" if your Apps Script expects token as query parameter
NEXT_PUBLIC_USE_QUERY_TOKEN=false

# Optional: Application origin for CORS (production only)
# In development, CORS is automatically handled
NEXT_PUBLIC_APP_ORIGIN=https://your-app.vercel.app

# Optional: Vercel URL (auto-set by Vercel, no need to configure locally)
# NEXT_PUBLIC_VERCEL_URL=your-app.vercel.app

# Optional: seed multiple auth users with bcrypt hashes (JSON array)
# AUTH_USERS_JSON=[{"username":"admin","role":"Admin","passwordHash":"$2b$10$..."},{"username":"staff1","role":"Staff","passwordHash":"$2b$10$..."}]

# Optional: Google Drive folder IDs for image uploads
# If not set, default folders will be used
# DRIVE_FOLDER_CARS=your-cars-folder-id
# DRIVE_FOLDER_MOTORCYCLES=your-motorcycles-folder-id
# DRIVE_FOLDER_TUKTUK=your-tuktuk-folder-id

# Optional: Apps Script upload token for image uploads
# If not set, image uploads will fail
# APPS_SCRIPT_UPLOAD_TOKEN=your-upload-token

# ============================================
# Setup Instructions
# ============================================

# 1. Copy this file to .env.local:
#    cp .env.local.example .env.local

# 2. Replace YOUR_SCRIPT_ID with your actual Google Apps Script ID

# 3. Generate a random SESSION_SECRET

# 4. Restart your development server:
#    npm run dev

# ============================================
# Troubleshooting
# ============================================

# If you see "Failed to load vehicles":
# 1. Check that NEXT_PUBLIC_API_URL is set correctly
# 2. Verify the Google Apps Script is deployed and accessible
# 3. Check browser console for CORS errors
# 4. Ensure the Apps Script has proper permissions

# For local development issues:
# 1. Make sure port 3000 is not in use
# 2. Check that npm run dev is running
# 3. Try clearing browser cache and cookies
